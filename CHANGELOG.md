# Changelog

## v0.6.0

- 固定资料唯一入口:`labs/` 放实际作业,`videos/` 放逐集介绍/字幕,`readings/` 放论文,`coursepacks/` 只放课程映射与官方 lecture 静态原件。
- 禁止在 coursepack 内重复创建 `labs/assignments/videos/readings/supplements`;外部作业 submodule 直接落到根级 `labs/`，不用软链接伪装入口。
- 播放列表每集必须有真实字幕依据的介绍、来源、主线/预习/拓展角色与 Stage 映射;只有链接或字幕不算完成。
- 新增批量拉取纪律:先展示最终目录树,先验证 1 个 Lab + 1 个视频再批量,完成前检查入口非空、非软链接、无旧路径与半移动 submodule。
- CLI 新增 `audit`;`course` 不再制造重复资料子目录,URL syllabus 保存为 `syllabus.url`;`video` 新增 overview/stage/role 字段。
- smoke test 新增规范目录、URL syllabus、视频介绍和 layout audit 验证。
- 同步本地既有修订:读字幕只用于核对、不构成学习期预讲许可;“是否学过/讨论过/完成”以 dialogue/raw/git history 优先于派生的 notes/state。

## v0.4.0(对照 v0.3.0 审计报告修订)

边界(A 类):
- A1 定位三不与自主性前提写入 README 首段与 SKILL.md §1;新增负功能清单(§1.1)
- A2 新增执行守门(§5.2)与异议义务(§5.3,反迁就条款,objection: 留痕);§14"目标太大自动压缩"改为告知+用户决定
- A3 理解核对改为锚定格式(§8):资料依据字段、"与资料不一致的"、模型知识只定位不判准
- A4 移除六级水平标签,起点改为证据锚定描述(§4.2、profile 模板)
- A5 模式判定改为可评测性判据,学科名降为示例(§3)
- A6 semi 模式并入双模式:可逐步核对的推导归 strong,其余归 qa;qa 收缩为纯答疑
- A7 移除 portfolio 模式与展示导向输出;实验过程记录保留于 labs/ 与 experiments/

需求(B 类):
- B1 诊断降为 2-3 道轻量探测题,明示可跳过(§4.2、plan 模板)
- B2 资料阀改为主线 1 + 补充 ≤2(§6、resources 模板)
- B3 视频成本三档 + 官方幻灯片原件优先(references/multimodal-video.md);video_README 禁止通读 keyframes/
- B4 新增 lab/评测机纪律簇(references/labs-and-checkpoints.md):课程 lab 优先且跨源、本地验证、连带 handout、不抓 solutions、先写后做、对拍、通过≠理解;CLI 新增 lab 脚手架
- B5 移除"先说理解"前置门槛:qa 直接答;strong 中复述仅为核对动作
- B6 恢复间隔热身(仅 strong、可拒);同一学期版本配套规则、拼接衔接与记号检查、按用户在读资料的记号作答;无联网降级为"类型+关键词"清单

工程(C 类):
- C1 SKILL.md 增加 YAML frontmatter(含冷启动与续学两类触发);skill.json 更新
- C2 渐进披露:核心 SKILL.md 压至 ~300 行,细则移入 references/;删除低价值存根模板(syllabus/readings)
- C3 脚本修复:course 死代码、transcript 非文件静默写入、append 改 with、git 身份缺失提示、嵌套仓库提醒、video 新增 --mode scene

新增(本轮讨论):
- mission.md:学习任务契约,属于用户;异议义务以 mission 为基准;三层所有权(mission 用户 / plan 守门 / 资料 agent 拍板可查验)
- entry 命令(可选):由 plan.md + resources.md 派生的 index.html 纯链接聚合页,无任何进度元素,按需重新生成

## v0.4.1

- 修复学习期开场过度主动:§11.1 改为"汇报状态 + 指出 plan 下一步 + 停",禁止选项菜单与核对预告
- §2.2 新增选择架构条款:agent 设计选项集即替学习者圈定决策空间,微观导航属于学习者
- §3.1 核对不预告、不排期;§13 学习期开场不超过三句
- 修正 §11.1 阅读清单漏掉 mission.md 的不一致

## v0.4.2

- 学习期改为话语类型分流(§7):提问→答疑;陈述理解/体会→对照资料原意核对;问"接下来学什么"→单数建议;其余等待
- 收回 strong 模式"恰当时机主动核对"许可:核对触发收敛为三种——学习者陈述、学习者请求、plan 内已确认的检查点(§3.1)
- 对自发陈述的核对只核对陈述本身,不追加考题;迁移题仅用于检查点与受邀核对
- qa 模式同步:陈述触发锚定核对,其余保持纯答疑(§3.2)

## v0.5.0

- 确立核心协议 §2.2"检查点是邀约,不是关卡"(交付—提交—检查):agent 交付检查点后不再提起;学习者提交才检查;未提交而进入下一阶段时提醒一次,不强制;坚持跳过记录为学习者的主动选择(记事实,不评判)
- strong/qa 在检查点上完全统一为同一协议,差异只剩检查点形态(可机判 lab vs 自查清单)
- §3.1 核对触发第三种由"到达检查点"改为"学习者提交检查点";§5.4 计划调整时机改为提交结果或宣布进入下一阶段
- §7 分流表新增"提交检查点→受邀检查""宣布进入下一阶段→提醒一次后尊重";state 模板增加检查点事实行

## v0.5.1

- 澄清模式语义(§3 与 README):"强"指核对工具的严格性(产出可被客观检验),不指强制性;两种模式的检查点同为 §2.2 邀约
- 修掉 README 残留的 v0.4.0 旧表述("agent 可主动核对理解"),与 v0.4.2 之后的触发规则保持一致
